# 自动提交跑步技术方案

## 1. 背景与目标

当前项目已经有两套相关能力：

1. 前端手动提交跑步记录；
2. 后端 `server` + `cron` + `SQLite` 的云端定时架构（用于社团签到/签退与自动报名）。

本方案目标是新增“每日自动提交跑步”能力，要求：

1. 用户主动开启/关闭；
2. 用户可配置时间、地图、里程；
3. 页面关闭后仍可执行（后端常驻）；
4. 每天最多自动提交一次（幂等）；
5. 执行结果可查询、可排障。

---

## 2. 技术栈

### 2.1 前端

1. `Vue 3`：配置界面与状态管理；
2. `Vite`：构建与环境变量注入；
3. `fetch`：调用后端配置接口；
4. 复用现有配置方式：`VITE_CLUB_SCHEDULER_BASE`（或后续统一命名为 `VITE_SCHEDULER_BASE`）。

### 2.2 后端

1. `Node.js` + `Express`：提供规则与日志 API；
2. `node-cron`：分钟级扫描与触发；
3. `sql.js / SQLite`：保存任务规则和执行日志；
4. `luxon`：时区与日期计算；
5. `crypto` + 现有 `tokenVault`：敏感 token 加密存储；
6. `winston`（现有）：结构化日志输出。

### 2.3 部署

1. `PM2`（推荐）保持 `server/index.js` 常驻；
2. `Nginx`（可选）统一反向代理前后端；
3. `.env` 管理运行参数（端口、密钥、数据库路径等）。

---

## 3. 实现原理

## 3.1 总体链路

```text
用户在前端保存“每日自动提交”规则
  ↓
后端校验并保存规则（含加密 token）
  ↓
node-cron 每分钟扫描启用规则
  ↓
命中当天触发窗口后尝试执行提交
  ↓
写入运行日志（ok / external / error）
```

### 3.2 为什么必须后端执行

若依赖前端定时器（`setTimeout` / `setInterval`）会有天然缺陷：

1. 页面关闭后任务停止；
2. 浏览器休眠/后台限制造成漏执行；
3. 难以保证幂等与可观测性。

后端常驻进程可提供：

1. 稳定执行窗口；
2. 持久化任务状态；
3. 可审计日志；
4. 标准化失败重试。

### 3.3 触发策略

每日自动提交以“规则驱动”执行：

1. 规则 `enabled = 1`；
2. 当前时区日期命中当日执行时间；
3. 当日未成功执行（幂等约束）；
4. 调用提交接口并记录结果。

### 3.4 选择参数来源

提交所需参数由规则提供：

1. `map_id`：用户选择地图；
2. `distance_m`：用户配置里程；
3. `token_enc`：用户 token（加密存储）；
4. 可选扩展：`route`、速度波动、随机区间策略。

---

## 4. 数据库设计

## 4.1 规则表：`daily_run_rules`

建议字段：

```sql
CREATE TABLE IF NOT EXISTS daily_run_rules (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_id INTEGER NOT NULL UNIQUE,
  token_enc TEXT NOT NULL,
  school_id INTEGER,
  map_id TEXT NOT NULL,
  distance_m INTEGER NOT NULL,
  cron_expr TEXT NOT NULL,
  timezone TEXT NOT NULL DEFAULT 'Asia/Shanghai',
  enabled INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

## 4.2 执行日志表：`daily_run_runs`

建议字段：

```sql
CREATE TABLE IF NOT EXISTS daily_run_runs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  rule_id INTEGER NOT NULL,
  run_date TEXT NOT NULL,
  status TEXT NOT NULL,
  message TEXT,
  response_code INTEGER,
  response_brief TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY (rule_id) REFERENCES daily_run_rules(id) ON DELETE CASCADE,
  UNIQUE (rule_id, run_date)
);
```

状态建议：

1. `running`：已领取执行；
2. `ok`：提交成功；
3. `external`：外部已完成/重复提交；
4. `error`：执行失败（网络、token、参数等）。

---

## 5. 后端接口设计

新增路由：`/api/daily-run`

1. `GET /api/daily-run/me`
   - 获取当前用户规则；
2. `POST /api/daily-run`
   - 保存/更新规则；
3. `DELETE /api/daily-run/me`
   - 关闭并删除规则；
4. `GET /api/daily-run/runs/today`
   - 管理员查看当日执行日志（`X-Scheduler-Admin`）。

---

## 6. 调度与执行细节

### 6.1 调度器

`dailyRunCron` 每分钟执行一次：

1. 查询 `enabled=1` 规则；
2. 按 `timezone + cron_expr` 判断是否到点；
3. 到点后执行 `maybeRunDailySubmit(...)`。

### 6.2 幂等保护（关键）

依赖 `UNIQUE(rule_id, run_date)` + `INSERT OR IGNORE`：

1. 同一用户同一天只会“领取”一次任务；
2. 避免多实例/重复扫描导致重复提交。

### 6.3 失败重试

建议策略：

1. `ok/external`：当天不再重试；
2. `error`：可设置最多重试 `N` 次（如 2 次），间隔 10 分钟；
3. 超限后维持 `error`，等待次日新窗口。

---

## 7. 前端接入原则

1. 不再依赖旧 `autorun` 独立服务；
2. 统一走当前 `server` 的 API；
3. 配置失败时明确提示“调度服务地址未配置”并给出 `.env` 修复指引；
4. 保存规则时先本地校验（地图/里程/时间），再提交后端。

---

## 8. 环境变量建议

前端（`app/.env`）：

```env
VITE_CLUB_SCHEDULER_BASE=http://127.0.0.1:8787
VITE_CLUB_SCHEDULER_USE_DEV_PROXY=true
```

后端（`server/.env`）：

```env
HOST=0.0.0.0
PORT=8787
SCHEDULER_DB_PATH=server/data/club_scheduler.sqlite
SCHEDULER_ENCRYPTION_KEY=请设置64位十六进制密钥
SCHEDULER_ADMIN_SECRET=请设置管理密钥
```

---

## 9. 最小可行版本（MVP）

第一版建议只做：

1. 每日固定时刻提交一次；
2. 地图 + 里程可配置；
3. 后端幂等保证；
4. 日志可查询；
5. 可开启/关闭。

在此基础上再迭代随机化策略、失败告警、批量账号管理等高级能力。

